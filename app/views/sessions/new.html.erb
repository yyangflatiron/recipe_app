Please sign in!

<%= flash[:message]%>

<%= form_tag "/sessions" do %>



<%= label_tag "Name" %>
<%= text_field_tag :name %>
<%= label_tag "Password" %>
<%= password_field_tag :password %>
<%= submit_tag "Sign in" %>

<% end %>

<!-- Need a form with Username and Password
	1. user does not exist
		take params, look for user, cannot find
		[flash_message] cannot find user with that name, "try again"

	2. enter wrong password
		take params, find user, check user.password_digest against password_digest of submitted password, if they don't match
		[flash_message] password does not match "try again" 
		
		^session[:user_id] = @user.id -->